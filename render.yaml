services:
  - type: web
    name: datanetra-ai
    runtime: python
    buildCommand: python3 -m venv /opt/venv && /opt/venv/bin/pip install --upgrade pip && /opt/venv/bin/pip install -r requirements.txt
    startCommand: /opt/venv/bin/python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
