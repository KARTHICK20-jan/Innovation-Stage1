services:
  - type: web
    name: datanetra-ai
    runtime: python
    buildCommand: pip3 install -r requirements.txt
    startCommand: pip3 install --break-system-packages -r requirements.txt && python3 app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
